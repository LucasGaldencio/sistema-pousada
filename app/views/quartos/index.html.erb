<p style="color: green"><%= notice %></p>

<h1>Quartos</h1>

<div id="quartos">
  <% @quartos.each do |quarto| %>
    <%= render quarto %>
    <p>
      <%= link_to "Mostrar detalhes do quarto", quarto %>
    </p>
  <% end %>
</div>

<%= link_to "Novo quarto", new_quarto_path %>

<!-- Botão para voltar para a Home -->
<div class="actions">
  <%= link_to "Voltar para Home", home_path, class: "btn btn-primary" %>
</div>