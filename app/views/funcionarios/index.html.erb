<p style="color: green"><%= notice %></p>

<h1>Funcionarios</h1>

<div id="funcionarios">
  <% @funcionarios.each do |funcionario| %>
    <%= render funcionario %>
    <p>
      <%= link_to "Mostrar detalhes do funcionario", funcionario %>
    </p>
  <% end %>
</div>

<%= link_to "Novo funcionario", new_funcionario_path %>

<!-- Botão para voltar para a Home -->
<div class="actions">
  <%= link_to "Voltar para Home", home_path, class: "btn btn-primary" %>
</div>